import { observer } from "mobx-react-lite";
import { Text, View } from "react-native";

export const Home = observer(() => {
  return (
    <View>
      <Text>home</Text>
    </View>
  );
});
